<h1>Continents Index</h1>
<a href="/continents/new">New Continent</a>

<% continents.forEach(continent => { %>
<div class="card">
  <a href="/continents/<%= continent.id %>"
    ><%= continent.name %> has a population of <%= continent.size %></a
  >
</div>
<% }) %>
